# 📜 Changelog — ADVisualizer

## [2.1.0] - 2026-02-27
### Added
- "Matrix View" in Excel export with conditional formatting and group colors.
- v2.0.0 Master Agent framework integration.

### Changed
- **BREAKING**: Reorganized project structure (scripts moved to `scripts/`, documentation moved to `docs/`).
- Standardized all modules (`.ps1` -> `.psm1`) and added public API exports.
- Improved `Export-ADUserGroups.ps1` as a modular entry point.

### Fixed
- Invalid `.gitmodules` configuration (moved references to `docs/SOURCES.md`).

## [2.0.0] - 2026-02-24
### Added
- Initial modularization of AD processing logic.

---
*Format based on Keep a Changelog.*
